{
  "name": "lets-go",
  "version": "1.0.0",
  "description": "Family activity tracker — What should we do this weekend?",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c bgCyan.white,bgMagenta.white \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "node server/server.js",
    "db:seed": "node server/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
